name: Publish @accesslint/mcp

on:
  push:
    tags: ["mcp/v*"]

env:
  TURBO_TOKEN: ${{ secrets.TURBO_TOKEN }}
  TURBO_TEAM: ${{ secrets.TURBO_TEAM }}

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2

      - uses: actions/setup-node@v5
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org

      - run: bun install

      - run: npx turbo run build --filter=...@accesslint/mcp

      - run: bun run test
        working-directory: mcp

      - name: Replace workspace:* with caret ranges of published versions
        working-directory: mcp
        # Caret range (^X.Y.Z) means future patch releases of @accesslint/core
        # flow through to fresh `npm install @accesslint/mcp` runs without
        # needing an MCP republish. The runtime CDN URL is built from
        # whatever core version npm resolved at install time.
        run: |
          node -e "
          const fs = require('fs'), path = require('path');
          const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
          for (const sec of ['dependencies', 'devDependencies', 'peerDependencies']) {
            for (const [name, ver] of Object.entries(pkg[sec] ?? {})) {
              if (ver === 'workspace:*') {
                const dir = name.split('/')[1];
                const dep = JSON.parse(fs.readFileSync(path.join('..', dir, 'package.json'), 'utf8'));
                pkg[sec][name] = '^' + dep.version;
              }
            }
          }
          fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      - run: npm publish --provenance --access public
        working-directory: mcp

      - name: Create GitHub release
        run: gh release create "$GITHUB_REF_NAME" --generate-notes --title "@accesslint/mcp v${GITHUB_REF_NAME#mcp/v}"
        env:
          GH_TOKEN: ${{ github.token }}
